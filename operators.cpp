#include<iostream>
using namespace std;
int main()
  {
    int x,y;
    cin>>x>>y;
    if(x<y)
     cout<<x<<" is smaller than "<<y;
     if(x>y)
     cout<<x<<" is greater than "<<y;
     if(x=y)
     cout<<x<<" is equal "<<y;
    return 0;
  }
